# # .husky/pre-push
# #!/bin/sh
# . "$(dirname "$0")/_/husky.sh"

# echo "Fetching latest changes..."
# git fetch

# LOCAL=$(git rev-parse @)
# REMOTE=$(git rev-parse @{u})

# if [ "$LOCAL" != "$REMOTE" ]; then
#   echo "❌ Your branch is behind the remote. Run: git fetch && check changes."
#   exit 1
# fi

# echo "✅ Branch is up to date, pushing..."
